import fs from'node:fs';let bad=false;const r=f=>fs.readFileSync(f,'utf8'),m=(x,s)=>{if(!x){console.error('FAIL '+s);bad=true}};const p=r('probe-history-v1.js'),w=r('watchtower-v1.js');for(const x of ['watchtower_probe_history','recordProbeHistory','probeTrends','availabilityPct','avgLatencyMs','pruneProbeHistory'])m(p.includes(x),`history ${x}`);for(const x of ['recordProbeHistory','probeTrends','trends','trend_${t.service}_availability'])m(w.includes(x),`Watchtower trend ${x}`);if(bad)process.exit(1);console.log('PASS Gate 5 persisted probe history and 24h availability/latency trends');
