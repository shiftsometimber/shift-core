export default {async fetch(request,env){const u=new URL(request.url);if(!['GET','HEAD'].includes(request.method))return new Response('Read-only preview',{status:405});if(u.pathname.startsWith('/v1/'))return new Response('No live API in this preview',{status:404});if(['/','/patient-intake','/treatment-checkout'].includes(u.pathname))u.pathname='/patient-intake.html';const r=await env.PREVIEW_ASSETS.fetch(new Request(u,request));const headers=new Headers(r.headers);headers.set('x-robots-tag','noindex, nofollow');headers.set('cache-control','no-store');headers.set('content-security-policy',"default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' blob: data:; connect-src 'none'; form-action 'none'; frame-ancestors 'self'; base-uri 'none'");headers.set('x-content-type-options','nosniff');return new Response(r.body,{status:r.status,headers})}};
