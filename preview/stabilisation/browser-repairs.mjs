import {readFileSync,writeFileSync} from 'node:fs';
import assert from 'node:assert/strict';
assert(process.env.GITHUB_REF==='refs/heads/preview/stabilisation-life-back-20260917');
function change(path,old,next){let s=readFileSync(path,'utf8');if(s.includes(next))return;assert(s.includes(old),'Expected source missing: '+path);writeFileSync(path,s.replace(old,next));}
change('preview/stabilisation/worker.mjs',"import staging from '../../work/staging/worker.mjs';","import staging from '../../work/staging/worker.mjs';\nimport {publicTickerAsset} from '../../public-navigation-policy.mjs';");
// A plain HTML form navigation under no-referrer can send Origin:null. Retain
// the same-origin check; send referrers to this preview origin only instead.
change('preview/stabilisation/worker.mjs',"'Referrer-Policy':'no-referrer'","'Referrer-Policy':'same-origin'");
change('preview/stabilisation/worker.mjs',"if(path==='/robots.txt')","const tickerAsset=publicTickerAsset(request);if(tickerAsset)return tickerAsset;\n if(path==='/robots.txt')");
// Public sign-in is a read-only shell review here, never a live sign-in surface.
// Remove its legacy account clients only in this sandbox; preserve its form,
// header, footer, styles and menu handler. Test accounts use /staging/sign-in.
change('preview/stabilisation/worker.mjs',"if(r.ok){let body=await r.text();body=body.replace(","if(r.ok){let body=await r.text();if(path==='/member-login')body=body.replace(/<script\\b[^>]*src=[\"'][^\"']*(?:member-|my-timber-|api-adapter-|turnstile-auth-|shift-me-api-)[^\"']*[\"'][^>]*>[\\s\\S]*?<\\/script>/gi,'');body=body.replace(");
// Supply the missing footer dependency to shells which did not have a footer.
// All declarations are scoped to the shared footer; no login/header resizing.
const css=`.site-footer{box-sizing:border-box;background:#050505;color:#e7e3da;border-top:1px solid #707762;padding:40px 0 24px;font:14px/1.6 Arial,sans-serif}.site-footer *{box-sizing:border-box}.site-footer>.site-wrap{width:min(1160px,calc(100% - 36px));margin:0 auto}.site-footer .footer-grid{display:grid;grid-template-columns:2fr repeat(5,minmax(0,1fr));gap:24px}.site-footer .footer-grid section{min-width:0}.site-footer .footer-brand img{display:block;width:260px;max-width:100%;height:auto}.site-footer h2{color:#e7e3da;font-size:14px;line-height:1.4;margin:0 0 12px}.site-footer p{margin:10px 0}.site-footer a{color:#e7e3da;overflow-wrap:anywhere}.site-footer .footer-grid a{display:block;margin:6px 0}.site-footer .footer-legal-links,.site-footer .accessibility-controls,.site-footer .footer-bottom{display:flex;flex-wrap:wrap;gap:12px 20px;margin-top:24px}.site-footer .footer-bottom{justify-content:space-between;padding-top:20px;border-top:1px solid #707762}.site-footer button{font:inherit;background:transparent;color:#e7e3da;border:1px solid #707762;border-radius:5px;padding:8px 12px}.site-footer a:focus-visible,.site-footer button:focus-visible{outline:2px solid #e7e3da;outline-offset:3px}@media(max-width:950px){.site-footer .footer-grid{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:560px){.site-footer .footer-grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:24px 18px}.site-footer .footer-brand{grid-column:1/-1}}`;
const insertion=" if(!html.includes('shift-recovery-v6.css')&&!html.includes('data-shared-footer-dependency'))html=html.replace('</head>',()=>'<style data-shared-footer-dependency>'+"+JSON.stringify(css)+"+'</style></head>');\n";
change('public-shell-contract.mjs'," // The old hub redirects; individual /medicine-news/* articles remain untouched.",insertion+" // The old hub redirects; individual /medicine-news/* articles remain untouched.");
change('preview/stabilisation/browser-proof.mjs',"await open(page,path);await page.locator('.site-header .menu-trigger').click();","row.publicPath=path;await open(page,path);await page.locator('.site-header .menu-trigger').click();");
change('preview/stabilisation/browser-proof.mjs',"row.status='fail';row.error=scrub(error.stack||error);","row.status='fail';row.finalUrl=page.url();row.bodyText=scrub(await page.locator('body').innerText().catch(()=>''));row.error=scrub(error.stack||error);");
change('preview/stabilisation/browser-proof.mjs',"catch(e){report.failures.push({phase:'fresh-review-and-boundaries',error:scrub(e.stack||e)})}","catch(e){await page.screenshot({path:dir+'/fresh-review-failure.png',fullPage:true}).catch(()=>{});report.failures.push({phase:'fresh-review-and-boundaries',url:page.url(),body:scrub(await page.locator('body').innerText().catch(()=>'')),error:scrub(e.stack||e)})}");
console.log('Applied only diagnosed preview fixes and added failure diagnostics; no tests weakened.');
