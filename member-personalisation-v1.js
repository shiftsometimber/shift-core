export function buildProductBrainOverrides(product,body={},brain={}){const prefs=brain?.member?.state?.preferences||{},feedback=brain?.behaviour?.feedback?.nay||[],nays=feedback.filter(x=>x.product===product).map(x=>String(x.entity_id));const merged={...body,exclude:[...new Set([...(Array.isArray(body.exclude)?body.exclude.map(String):[]),...nays])],brain_contract:brain?.contract||'one-shift-brain/v1'};if(body.preferences===undefined&&Object.keys(prefs).length)merged.preferences=JSON.stringify(prefs);if(product==='grub'){if(body.dislikes===undefined&&prefs.dislikes)merged.dislikes=list(prefs.dislikes);if(body.dietaryRequirements===undefined&&prefs.dietaryRequirements)merged.dietaryRequirements=list(prefs.dietaryRequirements)}else if(product==='fit'){if(body.limitations===undefined&&prefs.limitations)merged.limitations=list(prefs.limitations);if(body.equipment===undefined&&prefs.equipment)merged.equipment=prefs.equipment;if(body.location===undefined&&prefs.exercise_location)merged.location=prefs.exercise_location}return{merged,prefs,nays,currentIntentWins:true}}
function list(v){return Array.isArray(v)?v.join(', '):String(v)}
