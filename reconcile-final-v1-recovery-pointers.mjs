import fs from 'node:fs';
import path from 'node:path';

const root=path.resolve(process.env.RECONCILE_ROOT||'.');
const p=x=>path.join(root,x);
const counts=fs.readFileSync(p('docs/V1-RELEASE-BLOCKER-COUNTS.txt'),'utf8');
if(counts!=='A=0\nB=0\nC=3\nAUDIT_PASS=54\nAUDIT_AMBER=0\nAUDIT_BLOCKED=3\n')throw new Error('recovery pointers may update only after exact A=0 reconciliation');
const evidence='docs/evidence/2026-08-14-final-v1-gate2-production-pass.md';
if(!fs.existsSync(p(evidence)))throw new Error('final Gate 2 production PASS evidence missing');
const source=String(process.env.FINAL_V1_SOURCE_SHA||process.env.GITHUB_SHA||'production-proven-current-main');
const run=String(process.env.GITHUB_RUN_ID||'production-proof');

const active=`# Active recovery checkpoint — final Gate 2 production closure\n\n## Authority\n\nAuthoritative original audit: **57 total / 54 PASS / 0 AMBER / 3 BLOCKED / 0 unmapped**. Evidence: \`${evidence}\`.\n\nProduction accepted-authority source: \`${source}\`; production proof run: \`${run}\`.\n\nAll non-external original-audit rows are now PASS. The only original BLOCKED rows are **G5-001 / G5-002 / G5-003**, genuine provider/pharmacy/prescriber/verification dependencies. Do not imply unavailable clinical capability.\n\n## Gate 2 — CLOSED\n\nThe exact final human authority is production-proven:\n\n- Grub: **798 accepted recipes**, exact CoFID evidence, 28/28 unique seven-day served slots, zero legacy fallback, durable Nay exclusion across logout/fresh-login.\n- Fit: **26 accepted canonical movements / 1,326 accepted descendants**, zero legacy fallback, meaningful canonical breadth, durable Nay exclusion across logout/fresh-login.\n- Fit premium visuals: **26/26 accepted START/MOVE/FINISH SVGs** served over production HTTP.\n- Analytics: \`grub_plan_generated\` and \`fit_plan_generated\` retained.\n- Exact production publication: **2,124 rows**, partial publication forbidden.\n\nThe matrix/ledger/launch/blocker board have been atomically reconciled to A=0 from this exact production evidence.\n\n## Next non-clinical release step\n\n**Freeze new non-clinical product work now.** Continue only release closure:\n\n1. full RC regression on the reconciled current main;\n2. Dave + security/privacy + Watchtower + routes + accessibility/performance;\n3. targeted Shift Me/Today/G3/M04 proof if not already green on the promoted graph;\n4. final genuine-device hostile acceptance on iPhone Safari + Chrome mobile;\n5. fix release defects only;\n6. declare **NON-CLINICAL V1 READY** when those release-pack outcomes are green.\n\nDo not reopen Grub/Fit content production, human review, or alternative deployment architecture without genuine regression evidence.\n\n## Separate tails\n\n- Timber Mill issue #300 remains a separate manual static publication/visual-acceptance P0 outside the original 57 rows until its exact corrected ZIP is live.\n- G5-001/G5-002/G5-003 remain external clinical/provider BLOCKED.\n`;

const matt=`# Matt Final Acceptance Pack — V1\n\n## Content acceptance — COMPLETE\n\nGrub **8/8 PASS** and Fit **26/26 PASS** are already accepted and now production-proven through the exact accepted authority. **No further Grub/Fit content review is required.**\n\nThe original audit is **54 PASS / 0 AMBER / 3 BLOCKED**. The three BLOCKED rows are genuine external clinical/provider requirements, not unfinished non-clinical product work.\n\nEvidence: \`${evidence}\`.\n\n## Final human action now due — genuine-device hostile acceptance\n\nRun this only against the frozen release candidate.\n\n### iPhone Safari\n\nHome -> Register/Login -> My Shift -> Today -> Grub -> Fit -> Progress -> Progress Picture -> My Plans -> Shift AI -> logout/login.\n\nCheck: no horizontal scroll; authenticated shell remains intact; navigation opens/closes; notices do not intercept controls; forms/selects are production-grade; loading/empty/error states are intelligible; back/forward works; keyboard does not obscure critical controls.\n\n### Chrome mobile\n\nRepeat the same path and confirm no browser-specific clipping, pointer interception, stale session or blank state.\n\nRecord only failures with page/action/screenshot. If all pass, record **FINAL DEVICE ACCEPTANCE PASS**.\n\n## Timber Mill\n\nSeparate from the original audit: publish exactly \`ShiftSomeTimber-APPROVED-STORYBOARD-TIMBER-MILL-CORRECTED.zip\` through the existing Cloudflare static/manual path, then rerun its retained desktop + 390px production visual acceptance once. Do not rebuild it again.\n\n## READY rule\n\nFreeze new work -> release regression -> final genuine-device acceptance -> fix release defects only -> **NON-CLINICAL V1 READY**.\n`;

fs.writeFileSync(p('docs/ACTIVE-RECOVERY.md'),active);
fs.writeFileSync(p('docs/MATT-FINAL-ACCEPTANCE-PACK.md'),matt);
console.log('PASS A=0 recovery pointers reconciled to final production evidence');
