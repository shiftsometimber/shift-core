name: Reconcile G4-004 Board
on:
  push:
    branches: ['reconcile/g4-004-21pass']
permissions:
  contents: write
jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reconcile/g4-004-21pass
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
      - run: node reconcile-g4-004-board.mjs
      - run: node finish-line-gate.mjs
      - name: Commit reconciliation
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add docs/SHIFT-COMMISSIONING-REMEDIATION-MATRIX.md docs/LAUNCH-FINISH-LINE.md docs/COMMISSIONING-EVIDENCE.md finish-line-gate.mjs
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Reconcile G4-004 to 21 PASS / 33 AMBER'
          git push origin HEAD:reconcile/g4-004-21pass