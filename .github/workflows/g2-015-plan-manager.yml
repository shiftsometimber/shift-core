name: G2-015 My Plans Premium Manager Production Acceptance
on:
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  g2-015-plan-manager-production:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
      - name: Acquire short-lived Shift commissioning identity
        id: oidc
        shell: bash
        run: |
          raw=$(curl -fsSL -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=shift-production-commissioning")
          token=$(printf '%s' "$raw" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>process.stdout.write(JSON.parse(s).value))")
          echo "::add-mask::$token"
          echo "token=$token" >> "$GITHUB_OUTPUT"
      - name: Wait for production propagation
        run: sleep 30
      - name: Install Chromium
        run: |
          npm install --no-save --ignore-scripts playwright
          npx playwright install --with-deps chromium
      - name: Run rendered premium My Plans journey
        env:
          SHIFT_SITE_BASE: https://shiftsometimber.co.uk
          SHIFT_API_BASE: https://api.shiftsometimber.co.uk
          SHIFT_COMMISSIONING_OIDC: ${{ steps.oidc.outputs.token }}
          G2_015_EVIDENCE_DIR: g2-015-plan-manager-evidence
        run: node g2-015-plan-manager-production.mjs
      - name: Upload G2-015 premium evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: g2-015-plan-manager-evidence
          path: g2-015-plan-manager-evidence
          if-no-files-found: warn
          retention-days: 30
