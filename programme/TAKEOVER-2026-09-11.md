# Programme takeover — continuation of the accepted V1 brief

Owner: Codex. User instruction: take over Compare Competitor Critiques and complete its accepted work. Remote starting candidate: `3fe1fa578a63274fb82ea48e1b97249a463269f9`; local equivalent: `0c16b52`. Authoritative source remains this candidate branch and its pinned Pages baseline. No production release is implied.

## Recorded before edits

- PVT-01: Prevent delayed initial reads, saves and previews from restoring private state after the page has been cleared. A generation guard must cover every request and abort pending work on page exit/session invalidation. Add behavioural deferred-response tests; retain the separate browser-history gate.
- INT-01: Complete the narrow existing-record adapter with Journey purpose/status, recorded Life Back dates and weekly check-in counts/dates. Owner-scoped reads only; omit weight, private notes and health details. Missing optional tables report unavailable rather than false zero. Existing source records and accepted Programme plans remain byte-identical.
- OPS-01: Implement and verify explicit service expiry and bounded account/data operations within namespaced Programme modules, with no production execution. Preserve free tools and saved records on expiry.
- QA-01: Run the focused unit and actual-workerd/local-D1 contract suites, permitted browser checks, and protected-file hashes. Record initial failures and limits. Remote D1, actual Safari and named professional review are not inferred from local tests.

Permitted changes: `programme/**` and `frontend/member/assets/programme-v1/**`. A new route requires a recorded extension here before editing. Root Worker source, all protected Pages files, shared authentication, clinical/payment flows and deployment configuration remain unchanged.

Rollback: revert only this continuation's scoped commit. Do not roll back shared source or delete saved Programme data. Source branch save is authorised by Matt's prior approval. Release remains Matt's decision on a concrete reviewed candidate.

## Scope refinements before the next edits

- SEQ-01: Complete the first-month organisational sequence in the existing Today card. Stage labels follow real dates but never imply completion, invent reports or prescribe progression. A return after a gap and a saved review take precedence. No medical guidance is newly authored.
- OPS-01 detail: Add a server-only account operations module for explicit provisioning and bounded service expiry, plus authenticated read-only `/v1/programme/export` and a download link. Operator functions require a verified existing member ID supplied by the trusted operator and optimistic revisions; no public provisioning/entitlement endpoint. No real accounts are provisioned and no deletion executes.
- QA-01 limitation observed: reading the retained failed preview tab was rejected by Browser URL policy. Stop that browser action and use no alternate origin/browser/automation route to circumvent it. Browser-history/rendered/Safari verification remains unpassed.

## Verification outcome

84 focused tests, seven module-runtime/D1 contracts and six exact full-bundle checks pass. Five delayed-response cases are retained failing-before/passing-after. All 871 protected Pages files and root Worker/configuration are unchanged. This is one saved candidate, not release sign-off. Remaining externally constrained work is recorded in DELIVERY-3.md, CONTENT-REVIEW.md and OPERATIONS.md.
