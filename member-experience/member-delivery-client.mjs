export const deliveryMarkup=`<section class="md-section" id="memberDeliveryPanel" aria-labelledby="memberDeliveryTitle"><h3 id="memberDeliveryTitle">Delivery address</h3><p class="md-help">Choose your home address or save a separate delivery address. This does not change any order already placed. Check the address shown when placing a new order.</p><form id="memberDeliveryForm"><fieldset id="memberDeliveryFields" disabled><legend class="md-sr">Your preferred delivery address</legend><label for="memberDeliveryMode">Where should future deliveries go?<select id="memberDeliveryMode"><option value="home">Use my home address</option><option value="separate">Use a separate delivery address</option></select></label><p class="md-help" id="memberDeliveryHome"></p><div id="memberDeliverySeparate" hidden><label for="memberDeliveryRecipient">Recipient name<input id="memberDeliveryRecipient" autocomplete="shipping name" maxlength="160"></label><label for="memberDeliveryPostcode">Delivery postcode<input id="memberDeliveryPostcode" autocomplete="shipping postal-code" maxlength="12"></label><p class="md-help">Enter the full delivery address below.</p><label for="memberDeliveryAddress1">Address line 1<input id="memberDeliveryAddress1" autocomplete="shipping address-line1" maxlength="120"></label><label for="memberDeliveryAddress2">Address line 2 (optional)<input id="memberDeliveryAddress2" autocomplete="shipping address-line2" maxlength="120"></label><div class="md-grid"><label for="memberDeliveryTown">Town or city<input id="memberDeliveryTown" autocomplete="shipping address-level2" maxlength="100"></label><label for="memberDeliveryCounty">County (optional)<input id="memberDeliveryCounty" autocomplete="shipping address-level1" maxlength="100"></label></div></div></fieldset><div class="md-actions"><button id="memberDeliverySave" type="submit" disabled>Save delivery address</button><button id="memberDeliveryReload" type="button" class="md-secondary" hidden>Reload saved delivery address</button></div><p id="memberDeliveryStatus" role="status" aria-live="polite">Loading delivery details…</p></form></section>`;
export const deliveryRuntime=String.raw`(()=>{
'use strict';const form=document.getElementById('memberDeliveryForm');if(!form||form.dataset.bound)return;form.dataset.bound='true';const $=id=>document.getElementById(id),fields=$('memberDeliveryFields'),save=$('memberDeliverySave'),reload=$('memberDeliveryReload'),status=$('memberDeliveryStatus'),mode=$('memberDeliveryMode');
const ids={recipient:'memberDeliveryRecipient',address1:'memberDeliveryAddress1',address2:'memberDeliveryAddress2',town:'memberDeliveryTown',county:'memberDeliveryCounty',postcode:'memberDeliveryPostcode'};
let revision=null,pending=null,busy=false,dirty=false;
function message(s){status.textContent=s;}function controls(disabled){fields.disabled=disabled;save.disabled=disabled;}
function choice(){const separate=mode.value==='separate';$('memberDeliverySeparate').hidden=!separate;$('memberDeliveryHome').hidden=separate;for(const k of ['recipient','address1','town','postcode'])$(ids[k]).required=separate;}
function fill(b){if(!b?.ok||!b.delivery||typeof b.revision!=='string')throw Error('The saved delivery address could not be read.');revision=b.revision;mode.value=b.delivery.useHome?'home':'separate';for(const[k,id]of Object.entries(ids))$(id).value=b.delivery[k]||'';$('memberDeliveryHome').textContent=b.home?.address1?[b.home.recipient,b.home.address1,b.home.address2,b.home.town,b.home.county,b.home.postcode].filter(Boolean).join(', '):'Save a complete home address above first, or choose a separate delivery address.';dirty=false;choice();}
async function api(method='GET',body){let r;try{r=await fetch('/v1/member/details/delivery',{method,credentials:'include',cache:'no-store',headers:body?{'Content-Type':'application/json'}:{},...(body?{body:JSON.stringify(body)}:{}),signal:AbortSignal.timeout(15000)});}catch{const e=Error('Connection interrupted. Your edits are still here. Retry this save or reload the saved address.');e.uncertain=method==='PUT';throw e;}const b=await r.json().catch(()=>null);if(!r.ok||!b){const e=Error(b?.message||'The delivery request could not be confirmed.');e.status=r.status;e.uncertain=method==='PUT'&&(!b||b.error==='save_confirmation_unavailable');throw e;}return b;}
async function load(){if(busy)return;busy=true;controls(true);message('Loading your saved delivery address…');try{fill(await api());pending=null;controls(false);reload.hidden=true;message('Your saved delivery preference is shown above.');}catch(e){message(e.message);controls(true);reload.hidden=false;}finally{busy=false;}}
form.addEventListener('input',()=>{dirty=true;});mode.addEventListener('change',()=>{dirty=true;choice();});
reload.addEventListener('click',()=>{if(dirty&&!confirm('Discard unsaved delivery edits and reload the saved address?'))return;load();});
window.addEventListener('memberDetailsSaved',()=>{if(!dirty&&!busy)load();else message('Your home details were saved. Reload delivery details before saving these edits.');});
form.addEventListener('submit',async e=>{e.preventDefault();if(busy||save.disabled||!form.reportValidity())return;if(!pending)pending={delivery:{useHome:mode.value==='home',...Object.fromEntries(Object.entries(ids).map(([k,id])=>[k,$(id).value.trim()]))},revision,operationId:crypto.randomUUID()};busy=true;controls(true);reload.hidden=true;message('Saving delivery address…');try{fill(await api('PUT',pending));pending=null;controls(false);save.textContent='Save delivery address';message('Delivery address saved. Existing orders are unchanged.');window.dispatchEvent(new Event('memberDeliverySaved'));}catch(e){message(e.message);reload.hidden=false;if(e.uncertain){fields.disabled=true;save.disabled=false;save.textContent='Retry delivery save';}else{pending=null;controls(false);if([401,409].includes(e.status))save.disabled=true;}}finally{busy=false;}});
load();
})();`;
