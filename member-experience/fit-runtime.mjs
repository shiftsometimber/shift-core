// Existing approved Fit renderer, with account-backed activity and saved swaps.
export const fitRuntime="(function () {\n  \"use strict\";\n  const $ = (selector) => document.querySelector(selector),\n    esc = (value) =>\n      String(value ?? \"\").replace(\n        /[&<>\"']/g,\n        (char) =>\n          ({\n            \"&\": \"&amp;\",\n            \"<\": \"&lt;\",\n            \">\": \"&gt;\",\n            '\"': \"&quot;\",\n            \"'\": \"&#39;\",\n          })[char],\n      );\n  let memberPreferences = {},\n    fitJourney = { entries: {} };\n  function status(message, error = false) {\n    const node = $(\"#fitStatus\");\n    if (!node) return;\n    node.className = \"mp-status sf-status\" + (error ? \" error\" : \"\");\n    node.textContent = message;\n  }\n  function art(item = {}) {\n    const name = `${item.id || \"\"} ${item.name || \"\"}`.toLowerCase();\n    const demos = [\n      [\n        /squat|sit.?to.?stand|chair stand/,\n        \"Squat or sit-to-stand position\",\n        '<circle cx=\"28\" cy=\"17\" r=\"6\"/><path d=\"M29 23 39 41 58 43M39 41 26 57l-2 17m15-33 16 18M15 60h25M15 60v16\"/>',\n      ],\n      [\n        /push.?up|wall press|wall push/,\n        \"Push-up or wall-press position\",\n        '<circle cx=\"25\" cy=\"27\" r=\"6\"/><path d=\"m30 31 19 15 15 1M49 46 37 68m12-22 15 22M31 36 13 45M68 10v67\"/>',\n      ],\n      [\n        /row|pull/,\n        \"Row position\",\n        '<circle cx=\"28\" cy=\"18\" r=\"6\"/><path d=\"m30 24 13 22 18 3M43 46 30 69m13-23 17 24M35 31 17 42m0 0 14 2M10 38v12\"/>',\n      ],\n      [\n        /bridge/,\n        \"Glute bridge position\",\n        '<circle cx=\"14\" cy=\"48\" r=\"6\"/><path d=\"m20 48 20-17 19 17M40 31l-9 27m28-10 9 12M6 62h64\"/>',\n      ],\n      [\n        /plank/,\n        \"Plank position\",\n        '<circle cx=\"14\" cy=\"39\" r=\"6\"/><path d=\"m20 40 35 11 13 15M55 51 45 68M18 44 8 63M6 68h64\"/>',\n      ],\n      [\n        /dead bug/,\n        \"Dead bug position\",\n        '<circle cx=\"15\" cy=\"48\" r=\"6\"/><path d=\"m21 48 27 1m-18 0-10-22m20 22 16-22m-22 22-9 18m23-18 16 17M6 70h64\"/>',\n      ],\n      [\n        /bird.?dog/,\n        \"Bird-dog position\",\n        '<circle cx=\"20\" cy=\"35\" r=\"6\"/><path d=\"m26 38 24 8 18-9M42 43 25 61M50 46 61 66M24 39 8 27M5 70h65\"/>',\n      ],\n      [\n        /lunge|split squat|step.?up/,\n        \"Lunge or step position\",\n        '<circle cx=\"33\" cy=\"15\" r=\"6\"/><path d=\"m33 21 5 26 23 12M38 47 20 58 12 74m49-15 7 15M20 74h50\"/>',\n      ],\n      [\n        /calf raise/,\n        \"Calf-raise position\",\n        '<circle cx=\"35\" cy=\"15\" r=\"6\"/><path d=\"m35 21 1 31m0-19-15 10m15-10 17 9M36 52 26 70m10-18 15 17M18 73h42\"/>',\n      ],\n      [\n        /march|walk|walking|jog|run|cardio/,\n        \"Walking or marching position\",\n        '<circle cx=\"28\" cy=\"15\" r=\"6\"/><path d=\"m28 21 10 24 20 8M38 45 22 68m16-23 19 24M32 29 14 39\"/>',\n      ],\n      [\n        /shoulder press|overhead press/,\n        \"Overhead-press position\",\n        '<circle cx=\"35\" cy=\"25\" r=\"6\"/><path d=\"m35 31 1 23m-1-17-14-18m14 18 15-18M17 17h8m21 0h8M36 54 24 73m12-19 13 19\"/>',\n      ],\n      [\n        /curl/,\n        \"Arm-curl position\",\n        '<circle cx=\"35\" cy=\"17\" r=\"6\"/><path d=\"m35 23 1 29m-1-18-12 12-8-8m20-4 12 12 8-8M36 52 25 73m11-21 13 21\"/>',\n      ],\n      [\n        /hinge|deadlift|good morning/,\n        \"Hip-hinge position\",\n        '<circle cx=\"24\" cy=\"25\" r=\"6\"/><path d=\"m29 29 22 15 13 2M51 44 38 68m13-24 15 25M34 34 18 49M12 51h18\"/>',\n      ],\n    ];\n    const match = demos.find(([pattern]) => pattern.test(name));\n    const label =\n      match?.[1] || \"General movement guide — follow the written steps\";\n    const body =\n      match?.[2] ||\n      '<circle cx=\"35\" cy=\"18\" r=\"6\"/><path d=\"M35 24v28m0-18-18 10m18-10 18 10M35 52 22 73m13-21 14 21\"/>';\n    return `<svg viewBox=\"0 0 72 82\" role=\"img\" aria-label=\"${esc(label)}\"><g fill=\"none\" stroke=\"currentColor\" stroke-width=\"3\" stroke-linecap=\"round\" stroke-linejoin=\"round\">${body}</g></svg><small class=\"sf-demo-label\">${esc(label)}</small>`;\n  }\n  function brief(session, index) {\n    const groups = [\n        ...new Set(\n          (session.exercises || []).map((item) => item.group).filter(Boolean),\n        ),\n      ],\n      focus = groups.includes(\"legs\")\n        ? \"legs, balance and everyday strength\"\n        : groups.includes(\"push\") && groups.includes(\"pull\")\n          ? \"upper-body strength and posture\"\n          : groups.includes(\"push\")\n            ? \"pushing strength and shoulder control\"\n            : groups.includes(\"pull\")\n              ? \"back strength and posture\"\n              : groups.includes(\"core\")\n                ? \"core control and stability\"\n                : groups.includes(\"cardio\")\n                  ? \"fitness at a pace you can repeat\"\n                  : index === 0\n                    ? \"movement quality and a sensible starting point\"\n                    : index === 1\n                      ? \"useful strength and controlled effort\"\n                      : \"a balanced session you can build on\",\n      why = groups.includes(\"legs\")\n        ? \"The aim is to make stairs, walking and getting up and down feel easier.\"\n        : groups.includes(\"core\")\n          ? \"A steadier middle gives every other movement a better base.\"\n          : groups.includes(\"cardio\")\n            ? \"You should finish worked, not flattened, so coming back still feels realistic.\"\n            : \"We’re building strength you can use, without padding the session with pointless filler.\";\n    return `<div class=\"sf-coach\"><small>COACH’S BRIEF</small><strong>Today we’re working on ${esc(focus)}.</strong><p>${esc(why)} Controlled work, no heroics.</p></div>`;\n  }\n  function exercise(item, index, day) {\n    const metrics = [];\n    if (item.sets) metrics.push(`${item.sets} SETS`);\n    if (item.reps) metrics.push(`${item.reps} REPS`);\n    if (item.minutes) metrics.push(`${item.minutes} MIN`);\n    if (item.rest_seconds) metrics.push(`${item.rest_seconds} SEC REST`);\n    return `<article class=\"sf-exercise mp-exercise\" data-exercise-id=\"${esc(item.id || \"\")}\" data-exercise-group=\"${esc(item.group || \"\")}\" data-day=\"${day}\"><span class=\"sf-number\">${index + 1}</span><div class=\"sf-exercise-art\">${art(item)}</div><div class=\"sf-exercise-main\"><h4>${esc(item.name || \"Exercise\")}</h4><div class=\"sf-metrics\">${metrics.map((metric) => `<span>${esc(metric)}</span>`).join(\"\")}</div>${item.notes ? `<p>${esc(item.notes)}</p>` : \"\"}${(item.how || []).length ? `<details><summary>Show me how</summary><ol>${item.how.map((step) => `<li>${esc(step)}</li>`).join(\"\")}</ol></details>` : \"\"}<div class=\"sf-completion\"><button data-sf-complete=\"done\" type=\"button\">✓ Done</button><button data-sf-complete=\"skip\" type=\"button\">Skip today</button><select data-sf-skip-reason hidden aria-label=\"Why are you skipping this exercise?\"><option value=\"\">Choose a reason</option><option value=\"too_difficult\">Too difficult</option><option value=\"pain_or_discomfort\">Pain or discomfort</option><option value=\"no_equipment\">No equipment</option><option value=\"ran_out_of_time\">Ran out of time</option><option value=\"did_not_enjoy\">Didn’t enjoy it</option></select></div></div><div class=\"sf-exercise-actions\">${(item.how || []).length ? '<button data-sf-show type=\"button\">Show me how</button>' : \"\"}<button data-fit-vote=\"nay\" type=\"button\">Swap</button></div></article>`;\n  }\n  function sessionCard(session, index, total) {\n    const planned =\n        session.estimated_minutes || session.requested_minutes || \"\",\n      title = session.title || \"Session\";\n    return `<section class=\"sf-session\" data-session=\"${index}\" data-session-title=\"${esc(session.focus || title)}\" data-session-minutes=\"${esc(planned)}\"><div class=\"sf-session-head\"><div><span>DAY ${esc(session.day || index + 1)}</span><small>${esc(title)}</small><h3>${esc(session.focus || title)}</h3></div><div class=\"sf-ring\" style=\"--progress:${Math.max(1, Math.round(((index + 1) / total) * 100))}%\"><strong>${index + 1}</strong><small>OF ${total}</small></div></div>${brief(session, index)}<div class=\"sf-exercises\">${(session.exercises || []).map((item, itemIndex) => exercise(item, itemIndex, index)).join(\"\")}</div><button class=\"sf-start\" data-sf-start type=\"button\">START DAY ${esc(session.day || index + 1)} SESSION <span>→</span></button>${session.progression ? `<p class=\"sf-next\"><strong>Next time:</strong> ${esc(session.progression)}</p>` : \"\"}<div class=\"sf-debrief\" hidden><p class=\"sf-kicker\">SESSION BANKED</p><h3>How did that actually feel?</h3><div class=\"sf-session-summary\"></div><div class=\"sf-debrief-grid\"><label>EFFORT<select data-sf-review=\"effort\"><option value=\"\">Choose</option><option value=\"too_easy\">Too easy</option><option value=\"about_right\">About right</option><option value=\"hard\">Hard</option><option value=\"too_much\">Too much</option></select></label><label>YOUR BODY<select data-sf-review=\"body\"><option value=\"\">Choose</option><option value=\"good\">Good</option><option value=\"normal_effort\">Normal effort</option><option value=\"some_discomfort\">Some discomfort</option><option value=\"pain\">Pain</option></select></label><label>DO IT AGAIN?<select data-sf-review=\"again\"><option value=\"\">Choose</option><option value=\"yes\">Yes</option><option value=\"with_changes\">With changes</option><option value=\"no\">No</option></select></label><label>HOW DO YOU FEEL NOW?<select data-sf-review=\"mood\"><option value=\"\">Choose</option><option value=\"better\">Better</option><option value=\"same\">Same</option><option value=\"worse\">Worse</option></select></label></div><label class=\"sf-review-barrier\">WHAT GOT IN THE WAY?<select data-sf-review=\"barrier\"><option value=\"\">Nothing worth adding</option><option value=\"time\">Ran out of time</option><option value=\"difficulty\">Too difficult</option><option value=\"pain\">Pain or discomfort</option><option value=\"equipment\">Equipment</option><option value=\"instructions\">Instructions unclear</option><option value=\"motivation\">Lost motivation</option><option value=\"other\">Something else</option></select></label><label class=\"sf-review-note\">OPTIONAL SESSION NOTE<textarea data-sf-review=\"note\" placeholder=\"Optional — keep it short and honest\"></textarea></label><button class=\"sf-review-save\" data-sf-save-review type=\"button\">GET MY COACH’S VERDICT <span>→</span></button><div class=\"sf-verdict\" hidden></div></div></section>`;\n  }\n  function journeyKey(card) {\n    return `${new Date().toISOString().slice(0, 10)}:day-${Number(card.dataset.day) + 1}:${card.dataset.exerciseId}`;\n  }\n  function sessionJourneyKey(session) {\n    return `${new Date().toISOString().slice(0, 10)}:session-${Number(session.dataset.session) + 1}`;\n  }\n  function updateSession(card) {\n    const session = card.closest(\".sf-session\"),\n      all = [...session.querySelectorAll(\".sf-exercise\")],\n      done = all.filter((item) => item.dataset.completion === \"done\").length,\n      skipped = all.filter(\n        (item) => item.dataset.completion === \"skipped\",\n      ).length,\n      ring = session.querySelector(\".sf-ring\");\n    if (ring) {\n      ring.style.setProperty(\n        \"--progress\",\n        `${Math.round(((done + skipped) / Math.max(all.length, 1)) * 100)}%`,\n      );\n      ring.querySelector(\"strong\").textContent = done;\n      ring.querySelector(\"small\").textContent = `OF ${all.length} DONE`;\n    }\n    const start = session.querySelector(\"[data-sf-start]\"),\n      debrief = session.querySelector(\".sf-debrief\");\n    if (done + skipped === all.length && all.length) {\n      start.textContent =\n        done === all.length\n          ? \"SESSION DONE — USEFUL WORK BANKED\"\n          : \"SESSION FINISHED — PARTIAL STILL COUNTS\";\n      start.disabled = true;\n      session.classList.add(\"completed\");\n      debrief.hidden = false;\n      debrief.querySelector(\".sf-session-summary\").innerHTML =\n        `<strong>${done} of ${all.length} exercises completed</strong><span>${skipped ? `${skipped} skipped · ` : \"\"}${esc(session.dataset.sessionMinutes || \"—\")} planned minutes</span>`;\n      const review = fitJourney.sessionReviews?.[sessionJourneyKey(session)];\n      if (review) showVerdict(session, review);\n    }\n  }\n  function applyJourney() {\n    document.querySelectorAll(\"#fitOutput .sf-exercise\").forEach((card) => {\n      const record = fitJourney.entries?.[journeyKey(card)];\n      if (!record) return;\n      card.dataset.completion = record.status;\n      card.classList.toggle(\"done\", record.status === \"done\");\n      card.classList.toggle(\"skipped\", record.status === \"skipped\");\n      const done = card.querySelector('[data-sf-complete=\"done\"]'),\n        skip = card.querySelector('[data-sf-complete=\"skip\"]');\n      if (record.status === \"done\") {\n        done.textContent = \"✓ Done\";\n        done.setAttribute(\"aria-pressed\", \"true\");\n      }\n      if (record.status === \"skipped\") {\n        skip.textContent = \"Skipped today\";\n        skip.setAttribute(\"aria-pressed\", \"true\");\n      }\n      updateSession(card);\n    });\n  }\n  async function saveCompletion(card, state, reason = \"\") {\n    const key = journeyKey(card),\n      entries = { ...(fitJourney.entries || {}) };\n    entries[key] = {\n      status: state,\n      reason,\n      exerciseId: card.dataset.exerciseId,\n      group: card.dataset.exerciseGroup,\n      sessionDay: Number(card.dataset.day) + 1,\n      recordedOn: new Date().toISOString().slice(0, 10),\n      updatedAt: new Date().toISOString(),\n    };\n    const trimmed = Object.fromEntries(\n      Object.entries(entries)\n        .sort((a, b) =>\n          String(b[1].updatedAt).localeCompare(String(a[1].updatedAt)),\n        )\n        .slice(0, 500),\n    );\n    const nextJourney = { ...fitJourney, entries: trimmed };\n    const saved = await window.SST_API.saveFitActivity(nextJourney);\n    fitJourney = saved.fitJourney;\n    memberPreferences = { ...memberPreferences, fitJourney };\n    card.dataset.completion = state;\n    card.classList.toggle(\"done\", state === \"done\");\n    card.classList.toggle(\"skipped\", state === \"skipped\");\n    applyJourney();\n    try {\n      await window.SST_API.fitFeedback({\n        entity_id: card.dataset.exerciseId,\n        sentiment: state === \"done\" ? \"yay\" : \"nay\",\n        scope: \"today\",\n        context: {\n          surface: \"shift_fit_completion\",\n          action: state,\n          reason,\n          session_day: Number(card.dataset.day) + 1,\n        },\n      });\n    } catch (_) {\n      /* Journey is already safely saved; preference feedback is non-blocking. */\n    }\n    status(\n      state === \"done\"\n        ? \"Exercise done. Your journey has been updated.\"\n        : reason === \"pain_or_discomfort\"\n          ? \"Skipped and saved. Do not push through pain—swap it for something safer.\"\n          : \"Skipped for today. Partial sessions still count.\",\n    );\n  }\n  async function loadJourney() {\n    try {\n      const response = await window.SST_API.getFitActivity();\n      fitJourney = response.fitJourney;\n      if(response.plan?.sessions?.length)render(response.plan);\n      applyJourney();\n    } catch (error) {\n      status(error.message || \"Your saved Fit activity could not be loaded. Reload before making changes.\", true);\n    }\n  }\n  function verdictFor(review, done, total) {\n    if (review.body === \"pain\")\n      return `Stopping was the right call. Don’t push through pain. Your review is saved so you can use it when choosing or building another session.`;\n    if (review.effort === \"too_much\")\n      return `That was too much today. Fair enough. Choose a shorter or easier session next time, and use swap if a movement still does not suit.`;\n    if (review.effort === \"too_easy\")\n      return `Plenty left in the tank. Your review is saved; you can choose a slightly harder option next time.`;\n    if (review.barrier === \"time\")\n      return `Life happened. You still banked ${done} of ${total} exercises. Choose fewer minutes when you build the next session.`;\n    if (review.again === \"no\")\n      return `Not your thing. Your review is saved, but it will not automatically filter future plans; use swap if it appears again.`;\n    if (review.mood === \"better\")\n      return `You finished feeling better than you started. That matters. Your review is saved with this session.`;\n    if (done === total)\n      return `Session banked. Proper job. You completed everything without turning it into an Olympic trial.`;\n    return `Useful work banked. ${done} out of ${total} is still a session—not a failure. Your review is saved for you to refer back to.`;\n  }\n  function showVerdict(session, review) {\n    const box = session.querySelector(\".sf-verdict\"),\n      all = session.querySelectorAll(\".sf-exercise\").length,\n      done = session.querySelectorAll(\n        '.sf-exercise[data-completion=\"done\"]',\n      ).length;\n    [\"effort\", \"body\", \"again\", \"mood\", \"barrier\", \"note\"].forEach((name) => {\n      const field = session.querySelector(`[data-sf-review=\"${name}\"]`);\n      if (field) field.value = review[name] || \"\";\n    });\n    box.hidden = false;\n    box.innerHTML = `<small>SESSION REVIEW</small><p>${esc(review.verdict || verdictFor(review, done, all))}</p><span>Saved with your Fit activity. It does not automatically change future sessions.</span>`;\n    session.querySelector(\"[data-sf-save-review]\").textContent = \"REVIEW SAVED\";\n  }\n  async function saveReview(button) {\n    const session = button.closest(\".sf-session\"),\n      read = (name) =>\n        session.querySelector(`[data-sf-review=\"${name}\"]`)?.value || \"\",\n      review = {\n        effort: read(\"effort\"),\n        body: read(\"body\"),\n        again: read(\"again\"),\n        mood: read(\"mood\"),\n        barrier: read(\"barrier\"),\n        note: read(\"note\"),\n        recordedOn: new Date().toISOString().slice(0, 10),\n        updatedAt: new Date().toISOString(),\n      };\n    if (!review.effort || !review.body || !review.again || !review.mood)\n      throw new Error(\"Choose the four quick answers first.\");\n    const total = session.querySelectorAll(\".sf-exercise\").length,\n      done = session.querySelectorAll(\n        '.sf-exercise[data-completion=\"done\"]',\n      ).length;\n    review.verdict = verdictFor(review, done, total);\n    const nextJourney = {\n      ...fitJourney,\n      sessionReviews: {\n        ...(fitJourney.sessionReviews || {}),\n        [sessionJourneyKey(session)]: review,\n      },\n    };\n    const saved = await window.SST_API.saveFitActivity(nextJourney);\n    fitJourney = saved.fitJourney;\n    memberPreferences = { ...memberPreferences, fitJourney };\n    showVerdict(session, review);\n    status(\"Session review saved with your Fit activity.\");\n  }\n  function render(result) {\n    const plan = result?.plan || result || {},\n      sessions = plan.sessions || [],\n      output = $(\"#fitOutput\");\n    if (!sessions.length) {\n      output.innerHTML =\n        '<div class=\"sf-session\"><h3>No session returned.</h3><p>Shift could not build that plan just now. Please try again.</p></div>';\n      return;\n    }\n    const minutes =\n      plan.minutes_per_day ||\n      sessions[0]?.requested_minutes ||\n      sessions[0]?.estimated_minutes ||\n      \"—\";\n    output.innerHTML = `<div class=\"sf-week\"><span>THIS WEEK</span><b>•</b><span>${sessions.length} SESSION${sessions.length === 1 ? \"\" : \"S\"}</span><b>•</b><span>${Number(minutes) * sessions.length} MINUTES</span></div>${sessions.map((session, index) => sessionCard(session, index, sessions.length)).join(\"\")}`;\n    applyJourney();\n  }\n  const allowedLimitations = new Set([\n    \"knees\",\n    \"hips\",\n    \"back\",\n    \"shoulders\",\n    \"balance\",\n    \"low_impact\",\n    \"limited_floor_access\",\n    \"prefer_not_to_say\",\n  ]);\n  function fitContext() {\n    const selected = [\n      ...document.querySelectorAll('input[name=\"fitLimitation\"]:checked'),\n    ].map((field) => field.value);\n    if (\n      selected.some((value) => !allowedLimitations.has(value)) ||\n      selected.length > allowedLimitations.size\n    )\n      throw new Error(\"Choose limitations from the options shown.\");\n    if (selected.includes(\"prefer_not_to_say\") && selected.length > 1)\n      throw new Error(\n        \"Prefer not to say cannot be combined with another limitation.\",\n      );\n    const notes = $(\"#fitPrefs\").value.trim();\n    if (notes.length > 500)\n      throw new Error(\"Keep optional notes to 500 characters or fewer.\");\n    return {\n      limitations: { selected, notes: notes || null },\n      preferences: notes || undefined,\n    };\n  }\n  async function build() {\n    let context;\n    try {\n      context = fitContext();\n    } catch (error) {\n      status(error.message, true);\n      return;\n    }\n    const button = $(\"#fitGenerate\"),\n      previous = button.innerHTML,\n      data = {\n        days: Number($(\"#fitDays\").value) || 3,\n        minutes_per_day: Number($(\"#fitMinutes\").value) || 30,\n        location: $(\"#fitLocation\").value,\n        equipment: $(\"#fitEquipment\").value,\n        preferences: context.preferences,\n        limitations: context.limitations,\n      };\n    button.disabled = true;\n    button.textContent = \"Building your plan…\";\n    status(\n      \"Putting together sessions that fit the time and kit you actually have.\",\n    );\n    try {\n      const result = await window.SST_API.generateFit(data);\n      render(result);\n      status(\n        \"Your plan is ready. Start where you are and swap anything that does not suit.\",\n      );\n    } catch (error) {\n      status(error.message || \"Shift could not build the plan just now.\", true);\n    } finally {\n      button.disabled = false;\n      button.innerHTML = previous;\n    }\n  }\n  async function swap(button) {\n    let context;\n    try {\n      context = fitContext();\n    } catch (error) {\n      status(error.message, true);\n      return;\n    }\n    const card = button.closest(\".mp-exercise\"),\n      exclude = [...document.querySelectorAll(\".mp-exercise\")]\n        .map((item) => item.dataset.exerciseId)\n        .filter(Boolean);\n    button.disabled = true;\n    button.textContent = \"Swapping…\";\n    try {\n      const result = await window.SST_API.replaceFitExercise({\n        current_id: card.dataset.exerciseId,\n        persist: true,\n        session_index: Number(card.dataset.day),\n        exercise_index: [...card.parentElement.children].indexOf(card),\n        group: card.dataset.exerciseGroup,\n        exclude,\n        preferences: context.preferences,\n        limitations: context.limitations,\n        location: $(\"#fitLocation\").value,\n        equipment: $(\"#fitEquipment\").value,\n      });\n      const holder = document.createElement(\"div\"),\n        index = [...card.parentElement.children].indexOf(card);\n      holder.innerHTML = exercise(\n        result.exercise,\n        index,\n        Number(card.dataset.day) || 0,\n      );\n      card.replaceWith(holder.firstElementChild);\n      status(\"Exercise swapped. The rest of the session is untouched.\");\n    } catch (error) {\n      button.disabled = false;\n      button.textContent = \"Swap\";\n      status(error.message || \"Shift could not swap that exercise.\", true);\n    }\n  }\n  function init() {\n    const button = $(\"#fitGenerate\");\n    if (!button) return;\n    button.onclick = build;\n    $(\"#fitLimitations\").addEventListener(\"change\", (event) => {\n      const changed = event.target.closest('input[name=\"fitLimitation\"]');\n      if (!changed || !changed.checked) return;\n      const fields = [\n        ...document.querySelectorAll('input[name=\"fitLimitation\"]'),\n      ];\n      if (changed.value === \"prefer_not_to_say\")\n        fields.forEach((field) => {\n          if (field !== changed) field.checked = false;\n        });\n      else\n        fields.find((field) => field.value === \"prefer_not_to_say\").checked =\n          false;\n    });\n    loadJourney();\n\n    $(\"#fitOutput\").addEventListener(\n      \"click\",\n      async (event) => {\n        const reviewButton = event.target.closest(\"[data-sf-save-review]\");\n        if (reviewButton) {\n          reviewButton.disabled = true;\n          try {\n            await saveReview(reviewButton);\n          } catch (error) {\n            reviewButton.disabled = false;\n            status(error.message || \"Shift could not save that verdict.\", true);\n          }\n          return;\n        }\n        const completion = event.target.closest(\"[data-sf-complete]\");\n        if (completion) {\n          event.preventDefault();\n          event.stopImmediatePropagation();\n          const card = completion.closest(\".sf-exercise\");\n          if (completion.dataset.sfComplete === \"skip\") {\n            const select = card.querySelector(\"[data-sf-skip-reason]\");\n            select.hidden = false;\n            select.focus();\n            return;\n          }\n          completion.disabled = true;\n          try {\n            await saveCompletion(card, \"done\");\n          } catch (error) {\n            completion.disabled = false;\n            status(\n              error.message || \"Shift could not save that completion.\",\n              true,\n            );\n          }\n          return;\n        }\n        const swapButton = event.target.closest('[data-fit-vote=\"nay\"]');\n        if (swapButton) {\n          event.preventDefault();\n          event.stopImmediatePropagation();\n          swap(swapButton);\n          return;\n        }\n        const show = event.target.closest(\"[data-sf-show]\");\n        if (show) {\n          const details = show\n            .closest(\".sf-exercise\")\n            ?.querySelector(\"details\");\n          if (details) {\n            details.open = !details.open;\n            details.scrollIntoView({ behavior: \"smooth\", block: \"center\" });\n          }\n        }\n        const start = event.target.closest(\"[data-sf-start]\");\n        if (start) {\n          const session = start.closest(\".sf-session\");\n          session.classList.add(\"active\");\n          start.textContent = \"SESSION READY — START WITH EXERCISE 1\";\n          session\n            .querySelector(\".sf-exercise\")\n            ?.scrollIntoView({ behavior: \"smooth\", block: \"center\" });\n        }\n      },\n      true,\n    );\n    $(\"#fitOutput\").addEventListener(\n      \"change\",\n      async (event) => {\n        const select = event.target.closest(\"[data-sf-skip-reason]\");\n        if (!select || !select.value) return;\n        select.disabled = true;\n        try {\n          await saveCompletion(\n            select.closest(\".sf-exercise\"),\n            \"skipped\",\n            select.value,\n          );\n          select.hidden = true;\n        } catch (error) {\n          select.disabled = false;\n          status(error.message || \"Shift could not save that skip.\", true);\n        }\n      },\n      true,\n    );\n  }\n  window.addEventListener(\"load\", init, { once: true });\n})();\n";
