import {medicines, sources, REVIEWED_AT} from './data.mjs';
import {readWatchHealth} from './monitor.mjs';
import {TREATMENTS_ENTRY} from './preservation.mjs';

export const WATCH_PATH = '/treatment-centre/medicines-watch';
export const HEALTH_PATH = '/v1/medicines-watch/health';
const CANONICAL = 'https://shiftsometimber.co.uk' + WATCH_PATH;
const esc = value => String(value ?? '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const date = value => value && Number.isFinite(Date.parse(value)) ? new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'long',year:'numeric',timeZone:'Europe/London'}).format(new Date(value)) : 'Not yet recorded';
const time = value => value && Number.isFinite(Date.parse(value)) ? new Intl.DateTimeFormat('en-GB',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit',timeZone:'Europe/London',timeZoneName:'short'}).format(new Date(value)) : 'Not yet recorded';
const statusLabel = status => ({current:'Sources unchanged',verification_pending:'Verification pending',check_delayed:'Source check delayed',awaiting_review:'Update awaiting review'}[status] || 'Verification pending');

export const WATCH_STYLE = `<style data-medicines-watch-style>
.medicines-watch{--mw-cream:#e7e3da;--mw-ash:#707762;--mw-muted:#b7bcaa;box-sizing:border-box;background:#050505;color:var(--mw-cream);font:17px/1.65 Arial,Helvetica,sans-serif;padding:28px max(20px,calc((100% - 1140px)/2)) 80px;isolation:isolate}
.medicines-watch *{box-sizing:border-box}.medicines-watch [hidden]{display:none!important}.medicines-watch h1,.medicines-watch h2,.medicines-watch h3,.medicines-watch p{color:inherit;font-family:inherit}.medicines-watch h1{font-size:clamp(2.2rem,5.5vw,4.25rem)!important;line-height:1.05!important;letter-spacing:-.045em;margin:18px 0 24px!important;max-width:760px}.medicines-watch h2{font-size:clamp(1.4rem,3vw,2rem)!important;line-height:1.25!important;margin:0 0 12px}.medicines-watch h3{font-size:.78rem!important;line-height:1.4!important;text-transform:uppercase;letter-spacing:.09em;color:var(--mw-muted)!important;margin:0 0 10px}.medicines-watch p{font-size:inherit;line-height:inherit;margin:0 0 14px}.medicines-watch a{color:var(--mw-cream);text-underline-offset:4px}.medicines-watch :focus-visible{outline:3px solid var(--mw-cream);outline-offset:5px}.mw-breadcrumb{font-size:.85rem;margin:0 0 30px;color:var(--mw-muted)}.mw-eyebrow{font-size:.8rem!important;letter-spacing:.15em;text-transform:uppercase;font-weight:700;color:var(--mw-muted)!important}.mw-hero{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(200px,.6fr);gap:35px;align-items:center;padding:15px 0 35px}.mw-lead{max-width:650px;color:#d2d3c8!important;font-size:1.16rem!important}.mw-radar{width:100%;max-width:280px;justify-self:end;color:var(--mw-ash)}.mw-radar .mw-ray{stroke:#b7bcaa}.mw-overview{border:1px solid var(--mw-ash);border-radius:16px;background:#12150f;padding:24px;margin-bottom:28px}.mw-counts{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:20px;margin-bottom:20px}.mw-counts div+div{border-left:1px solid #454b3e;padding-left:24px}.mw-counts strong{display:block;font-size:2rem;line-height:1.3}.mw-counts span{font-size:.9rem;color:var(--mw-muted)}.mw-monitor{border-top:1px solid #454b3e;padding-top:16px;font-size:.88rem;color:#d2d3c8}.mw-monitor strong{color:var(--mw-cream)}.mw-monitor p{margin:4px 0}.mw-dot{display:inline-block;width:9px;height:9px;background:var(--mw-muted);border-radius:50%;margin-right:8px}.mw-filter{display:grid;grid-template-columns:2fr 1fr 1fr auto;gap:14px;align-items:end;margin:30px 0 12px}.mw-filter label{font-size:.8rem;font-weight:700;display:grid;gap:7px}.medicines-watch input,.medicines-watch select,.medicines-watch button{font:inherit;max-width:100%;min-width:0;min-height:48px;border:1px solid var(--mw-ash);border-radius:9px;padding:10px 12px;background:var(--mw-cream);color:#404935;-webkit-text-fill-color:#404935}.medicines-watch button{cursor:pointer;font-weight:700}.mw-result{font-size:.85rem!important;color:var(--mw-muted)!important;margin-bottom:18px!important}.mw-card{border:1px solid var(--mw-ash);border-radius:16px;background:linear-gradient(140deg,#151a11,#0a0d08);margin:0 0 18px;overflow:hidden;scroll-margin-top:110px}.mw-card summary{list-style:none;cursor:pointer;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:18px;align-items:center;padding:24px}.mw-card summary::-webkit-details-marker{display:none}.mw-card summary:after{content:'+';font-size:2rem;font-weight:400;line-height:1}.mw-card[open] summary:after{content:'−'}.mw-card summary h2{margin:10px 0 5px!important}.mw-card summary p{color:#c5c9ba;font-size:.92rem!important;margin:0}.mw-tag{display:inline-flex;align-items:center;gap:6px;border:1px solid var(--mw-ash);border-radius:6px;color:#d6daca;background:#242c1d;font-size:.72rem;font-weight:700;letter-spacing:.05em;padding:4px 9px;text-transform:uppercase}.mw-tag-research{background:#29251c;color:#e7d7bb;border-color:#9b8863}.mw-card-content{padding:0 24px 24px}.mw-card-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0 28px}.mw-cell{padding:22px 0;border-top:1px solid #424a39;min-width:0}.mw-cell p:last-child{margin-bottom:0}.mw-cell ul{padding-left:20px;margin:0}.mw-cell li{margin:0 0 7px}.mw-evidence{border-top:1px solid #424a39;padding-top:20px}.mw-evidence>summary{display:block!important;padding:0!important;font-weight:700;line-height:1.5}.mw-evidence>summary:after{content:' ↓'!important;font-size:1rem!important}.mw-evidence ul{padding:0;list-style:none;margin:18px 0 0}.mw-evidence li{padding:13px 0;border-top:1px solid #313a2b;overflow-wrap:anywhere;font-size:.92rem}.mw-evidence small{display:block;color:#b7bcaa;margin-top:4px;font-size:.8rem}.mw-review{margin:0 0 14px!important;font-size:.8rem!important;color:#b7bcaa!important}.mw-flag{border-left:3px solid #b7bcaa;background:#22271b;padding:10px 14px;margin:0 0 18px!important;font-size:.9rem!important}.mw-explainer{margin:35px 0 0;padding:26px;border:1px solid var(--mw-ash);border-radius:16px;background:#12150f}.mw-explainer-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:24px;margin:24px 0}.mw-explainer-grid strong{display:block;margin-bottom:7px}.mw-explainer-grid p{font-size:.95rem!important;color:#c5c9ba}.mw-note{font-size:.88rem!important;color:#c5c9ba!important}.mw-links{display:flex;flex-wrap:wrap;gap:20px;margin-top:24px}.mw-links a{font-size:.9rem}.mw-empty{padding:28px;border:1px solid var(--mw-ash);border-radius:12px}
@media(max-width:650px){.medicines-watch{padding:20px 18px 60px;font-size:16px}.mw-breadcrumb{margin-bottom:22px}.mw-hero{grid-template-columns:1fr;padding:0 0 25px;gap:0}.mw-radar{display:none}.mw-hero h1{font-size:2.65rem!important}.mw-overview{padding:18px}.mw-counts{gap:9px}.mw-counts div+div{padding-left:12px}.mw-counts strong{font-size:1.65rem}.mw-counts span{font-size:.75rem;line-height:1.35;display:block}.mw-filter{grid-template-columns:1fr 1fr;gap:12px}.mw-filter label:first-child{grid-column:1/-1}.mw-filter button{grid-column:1/-1}.mw-card summary{padding:20px 18px;gap:10px}.mw-card-content{padding:0 18px 20px}.mw-card-grid,.mw-explainer-grid{grid-template-columns:1fr}.mw-cell{padding:18px 0}.mw-explainer{padding:20px}.mw-explainer-grid{gap:10px}.mw-review{line-height:1.5!important}}
</style>`;

const RADAR = `<svg class="mw-radar" viewBox="0 0 300 300" aria-hidden="true" fill="none"><circle cx="150" cy="150" r="126" stroke="currentColor"/><circle cx="150" cy="150" r="94" stroke="currentColor"/><circle cx="150" cy="150" r="62" stroke="currentColor"/><path d="M150 9v282M9 150h282" stroke="currentColor" opacity=".45"/><path d="M150 150 243 66" class="mw-ray" stroke-width="3"/><circle cx="221" cy="88" r="5" fill="#b7bcaa"/><circle cx="78" cy="208" r="5" fill="#b7bcaa"/><rect x="130" y="117" width="39" height="70" rx="19.5" transform="rotate(35 150 150)" stroke="#b7bcaa" stroke-width="3"/><path d="m131 141 34 22" stroke="#b7bcaa" stroke-width="3"/></svg>`;

function sourceMarkup(source, health) {
 const state = health?.sources?.find(item => item.id === source.id);
 return `<li><a href="${esc(source.url)}" rel="noopener noreferrer">${esc(source.title || source.label || source.id)}</a><small>Reference reviewed ${date(source.reviewedAt)} · ${statusLabel(state?.status)}</small><small>Last successful source check: ${time(state?.lastSuccessAt)}${state?.lastFailureAt ? ' · Last failed check: '+time(state.lastFailureAt) : ''}</small></li>`;
}
function card(medicine, health, open) {
 const state = health?.medicines?.find(item => item.id === medicine.id);
 const authorised = medicine.status === 'authorised';
 const selectedSources = medicine.sourceIds.map(id => sources.find(s => s.id === id)).filter(Boolean);
 const access = medicine.access || {};
 return `<details class="mw-card" id="${esc(medicine.id)}" data-watch-card data-status="${esc(medicine.status)}" data-route="${esc(medicine.routeType || (/injection/i.test(medicine.route)?'injection':'oral'))}" data-search="${esc([medicine.name,medicine.ingredient,medicine.mechanism].join(' ').toLowerCase())}"${open?' open':''}><summary><div><span class="mw-tag${authorised?'':' mw-tag-research'}">${authorised?'UK authorised':'Investigational · not UK authorised'}</span><h2>${esc(medicine.name)}</h2><p>${esc(medicine.ingredient)} · ${esc(medicine.route)}</p></div></summary><div class="mw-card-content"><p class="mw-review">Reference reviewed ${date(medicine.reviewedAt || REVIEWED_AT)} · ${statusLabel(state?.status)}</p>${state?.status !== 'current' ? `<p class="mw-flag">${state?.status === 'awaiting_review' ? 'This reference needs review. The dated information below is the last reviewed reference; check the linked authority for the latest position.' : 'Some source checks are pending or delayed. Check the linked authority for the latest position.'}</p>` : ''}<div class="mw-card-grid"><section class="mw-cell"><h3>What it does</h3><p>${esc(medicine.mechanism)}</p><p>${esc(medicine.benefit)}</p></section><section class="mw-cell"><h3>UK position</h3><p>${esc(medicine.authorisation)}</p>${medicine.phase?'<p>'+esc(medicine.phase)+'</p>':''}</section><section class="mw-cell"><h3>Trade-offs and limits</h3><ul>${medicine.tradeoffs.map(text=>'<li>'+esc(text)+'</li>').join('')}</ul></section><section class="mw-cell"><h3>Access and availability</h3><p><strong>NHS — England:</strong> ${esc(access.nhsEngland)}</p><p><strong>Private access:</strong> ${esc(access.private)}</p></section></div><details class="mw-evidence"><summary>View evidence &amp; source checks</summary><ul>${selectedSources.map(source=>sourceMarkup(source,health)).join('')+(medicine.evidenceLinks||[]).map(source=>'<li><a href="'+esc(source.url)+'" rel="noopener noreferrer">'+esc(source.title)+'</a><small>Reference reviewed '+date(source.reviewedAt||REVIEWED_AT)+' · Additional evidence; automatic content check not available</small></li>').join('')}</ul></details></div></details>`;
}

export function watchMain(health = {}, query = new URLSearchParams()) {
 const current = (health.sources || []).filter(item => item.status === 'current').length;
 const q = (query.get('q') || '').slice(0,100).toLowerCase();
 const status = ['authorised','investigational'].includes(query.get('status')) ? query.get('status') : '';
 const route = ['oral','injection'].includes(query.get('route')) ? query.get('route') : '';
 const chosen = medicines.filter(m => (!status || m.status === status) && (!route || (m.routeType || (/injection/i.test(m.route)?'injection':'oral')) === route) && (!q || [m.name,m.ingredient,m.mechanism].join(' ').toLowerCase().includes(q)));
 const option = (value,label,selected) => `<option value="${value}"${selected===value?' selected':''}>${label}</option>`;
 return `<main id="main-content" class="medicines-watch" data-watch-version="1"><nav class="mw-breadcrumb" aria-label="Breadcrumb"><a href="/treatment-centre">Treatments</a> / Medicines &amp; Peptides Watch</nav><section class="mw-hero"><div><p class="mw-eyebrow">SHIFT Radar · UK focus</p><h1>Medicines &amp;<br>Peptides Watch</h1><p class="mw-lead">What’s authorised, what’s being studied, and what it means in the UK. Clear evidence. Honest limits.</p><a href="#how-we-check">How we keep this credible ↓</a></div>${RADAR}</section><section class="mw-overview" aria-label="Coverage and source status"><div class="mw-counts"><div><strong>${medicines.length}</strong><span>Medicines covered</span></div><div><strong>${medicines.filter(m=>m.status==='authorised').length}</strong><span>UK authorised</span></div><div><strong>${medicines.filter(m=>m.status==='investigational').length}</strong><span>Investigational</span></div></div><div class="mw-monitor"><p><span class="mw-dot"></span><strong>Weight-management focus</strong> · Reference review ${date(REVIEWED_AT)}</p><p>${current} of ${sources.length} monitored sources have current, unchanged checks. ${health.available === false ? 'The source monitor is temporarily unavailable.' : ''}</p><p>Latest check attempt: ${time(health.lastAttemptAt)}. <a href="${WATCH_PATH}">Refresh status</a></p></div></section><form class="mw-filter" method="get" action="${WATCH_PATH}" role="search" data-watch-filter><label>Find a medicine<input type="search" name="q" value="${esc(q)}" placeholder="Name or ingredient" maxlength="100"></label><label>UK status<select name="status">${option('','All statuses',status)+option('authorised','UK authorised',status)+option('investigational','Investigational',status)}</select></label><label>Medicine form<select name="route">${option('','All forms',route)+option('oral','Tablet / capsule',route)+option('injection','Injection',route)}</select></label><button type="submit">Apply filters</button></form><p class="mw-result" role="status" aria-live="polite">Showing ${chosen.length} of ${medicines.length} medicines</p><section aria-label="Medicine reference cards">${chosen.length ? chosen.map((m,i)=>card(m,health,i===0)).join('') : '<p class="mw-empty">No medicines match. <a href="'+WATCH_PATH+'">Clear filters</a></p>'}</section><section id="how-we-check" class="mw-explainer"><p class="mw-eyebrow">The evidence, kept honest</p><h2>Checked regularly. Reviewed separately.</h2><div class="mw-explainer-grid"><div><strong>1. Go to the source</strong><p>MHRA decisions, official product information, NHS guidance and original trial announcements. Every reference links to its evidence.</p></div><div><strong>2. Watch for change</strong><p>The source monitor attempts checks hourly. A changed, withdrawn, overdue or unreachable reference is flagged openly.</p></div><div><strong>3. Review the meaning</strong><p>A successful check does not renew the review date or confirm clinical advice. Changes need an evidence review before the wording changes.</p></div></div><p class="mw-note">This covers six selected weight-management medicines, not every medicine or peptide. UK authorisation, NHS funding and actual supply are different decisions. NHS access shown here relates to England; arrangements elsewhere in the UK can differ. Research results do not establish authorisation or guarantee an individual outcome.</p><p class="mw-note">General information, not individual medical advice. A clinician or pharmacist can help you understand suitability, risks and interactions. Do not use unapproved products sold as “research peptides”.</p><div class="mw-links"><a href="/shift-newsroom">Read SHIFT Newsroom updates →</a><a href="/editorial-standards#corrections">Report a correction</a><a href="/treatment-centre">Back to Treatments</a></div></section></main>`;
}

export function renderWatchDocument(html, health, query) {
 if (!/<main\b[\s\S]*?<\/main>/i.test(html)) throw new Error('Treatments shell main not found');
 const title = 'Medicines & Peptides Watch | SHIFT Treatments';
 const description = 'UK weight-management medicines explained with authorisation, access, emerging research, exact sources and visible review dates.';
 html = html.replace(/<main\b[\s\S]*?<\/main>/i, () => watchMain(health,query))
  .replace(/<script\b[^>]*\bsrc=["']\/assets\/treatment-guided-v1\.js(?:\?[^"']*)?["'][^>]*>[\s\S]*?<\/script>/gi,'')
  .replace(/<title>[\s\S]*?<\/title>/i,'').replace(/<meta\s+name=["']description["'][^>]*>/gi,'')
  .replace(/<link\s+rel=["']canonical["'][^>]*>/gi,'').replace(/<meta\s+(?:property|name)=["'](?:og:|twitter:)[^>]*>/gi,'')
  .replace(/<script\s+type=["']application\/ld\+json["'][^>]*>[\s\S]*?<\/script>/gi,'')
  .replace(/<body\b([^>]*)>/i,(_,attrs)=>'<body'+attrs.replace(/\sclass=(["'])(.*?)\1/i,(_,quote,classes)=>' class='+quote+classes.split(/\s+/).filter(c=>!c.startsWith('treatment-')).concat('medicines-watch-page').join(' ')+quote)+'>');
 const schema = {'@context':'https://schema.org','@type':'CollectionPage',name:title,description,url:CANONICAL,dateModified:REVIEWED_AT,publisher:{'@type':'Organization',name:'Shift Some Timber'}};
 return html.replace('</head>',`<title>${esc(title)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${CANONICAL}"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${CANONICAL}"><meta property="og:type" content="website"><meta property="og:image" content="https://shiftsometimber.co.uk/assets/og-default.jpg"><meta name="twitter:card" content="summary_large_image"><script type="application/ld+json">${JSON.stringify(schema).replace(/</g,'\\u003c')}</script>${WATCH_STYLE}</head>`);
}

export async function withMedicinesWatchEntry(response, request) {
 if (new URL(request.url).pathname.replace(/\/+$/,'') !== '/treatment-centre' || request.method !== 'GET' || !response.ok || !response.headers.get('content-type')?.includes('text/html')) return response;
 const html = await response.text();
 if (html.includes('SHIFT_MEDICINES_WATCH_ENTRY_START')) return new Response(html,response);
 if (!/<main\b[^>]*>/i.test(html)) throw new Error('Treatments entry anchor missing');
 const body = html.replace(/(<main\b[^>]*>)/i, () => html.match(/<main\b[^>]*>/i)[0]+TREATMENTS_ENTRY);
 const headers = new Headers(response.headers);headers.delete('Content-Length');headers.delete('ETag');headers.delete('Last-Modified');
 return new Response(body,{status:response.status,headers});
}

export async function medicinesWatchRoutes(request,env,{fetchImpl=fetch}={}) {
 const url = new URL(request.url), path=url.pathname.replace(/\/+$/,'');
 if (![WATCH_PATH,HEALTH_PATH].includes(path)) return null;
 if (!['shiftsometimber.co.uk','www.shiftsometimber.co.uk'].includes(url.hostname)) return null;
 if (!['GET','HEAD'].includes(request.method)) return new Response('Method not allowed',{status:405,headers:{Allow:'GET, HEAD'}});
 const health = await readWatchHealth(env);
 if (path === HEALTH_PATH) return new Response(request.method==='HEAD'?null:JSON.stringify(health),{headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'}});
 const upstream = new URL('https://projectshift.pages.dev/treatment-centre');
 const unavailable = () => new Response('The Medicines Watch page is temporarily unavailable. Please try again shortly.',{status:503,headers:{'Content-Type':'text/plain; charset=utf-8','Retry-After':'60'}});
 let base,html;
 try {
  base = await fetchImpl(upstream,{headers:{Accept:'text/html'},signal:AbortSignal.timeout(15000)});
  if (!base.ok) return unavailable();
  html = renderWatchDocument(await base.text(),health,url.searchParams);
 } catch { return unavailable(); }
 const headers = new Headers(base.headers);for (const name of ['Content-Length','ETag','Last-Modified']) headers.delete(name);
 headers.set('Content-Type','text/html; charset=utf-8');headers.set('Cache-Control','public, max-age=60, must-revalidate');headers.set('X-Shift-Medicines-Watch','1');
 return new Response(request.method==='HEAD'?null:html,{headers});
}
