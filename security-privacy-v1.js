export const SECURITY_PRIVACY_POLICY={session:{httpOnly:true,secure:true,sameSite:'Lax'},analytics:{denyKeys:['password','token','secret','email','phone','address','symptom','diagnosis','medication','name','postcode','dob']},uploads:{userScoped:true,deleteRequired:true},memberData:{exportRequired:true,deleteRequired:true},audit:{hqWritesRequired:true},secrets:{clientExposureForbidden:true}};
export function privacySafeProperties(input={}){const deny=new RegExp(SECURITY_PRIVACY_POLICY.analytics.denyKeys.join('|'),'i'),out={};for(const[k,v]of Object.entries(input).slice(0,40)){if(deny.test(k))continue;if(v===null||['string','number','boolean'].includes(typeof v))out[k]=typeof v==='string'?v.slice(0,300):v}return out}
export function assertMemberBoundary(resourceUserId,sessionUserId){if(!Number.isFinite(Number(resourceUserId))||!Number.isFinite(Number(sessionUserId))||Number(resourceUserId)!==Number(sessionUserId)){const e=new Error('member_boundary_violation');e.status=403;throw e}return true}
export function rejectMaliciousText(value,{max=5000}={}){const s=String(value??'');if(s.length>max)throw Object.assign(new Error('input_too_large'),{status:413});if(/[\u0000-\u0008\u000B\u000C\u000E-\u001F]/.test(s))throw Object.assign(new Error('invalid_control_character'),{status:400});return s}
export function securityCommissioningChecklist(){return['sessions','rate_limits','secrets','permissions','uploads','export_delete','audit_trail','analytics_privacy','member_isolation','input_limits'].map(id=>({id,required:true}))}
