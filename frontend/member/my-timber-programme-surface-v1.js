(function(){'use strict';const dialog=document.getElementById('mtProgrammeLive');if(!dialog)return;const targets={build:'#sgBuild',meals:'#sgDaily',feedback:'#sgTasteCard',start:'#sfBuild',plan:'#sfDaily'};function reveal(key){if(!dialog.open)dialog.showModal();dialog.scrollTop=0;let tries=0;const find=()=>{const target=dialog.querySelector(targets[key]||'.mp-panel');if(target){target.scrollIntoView({behavior:'auto',block:'start'});target.focus?.({preventScroll:true});return}if(tries++<30)setTimeout(find,100)};find()}document.addEventListener('click',event=>{const open=event.target.closest('[data-programme-open]'),close=event.target.closest('[data-programme-close]');if(close){dialog.close();return}if(!open)return;event.preventDefault();reveal(open.dataset.programmeOpen)});dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()})})();
