(function(){'use strict';const dialog=document.getElementById('mtProgrammeLive');if(!dialog)return;document.addEventListener('click',event=>{const open=event.target.closest('[data-programme-open]'),close=event.target.closest('[data-programme-close]');if(close){dialog.close();return}if(!open)return;event.preventDefault();if(!dialog.open)dialog.showModal();dialog.scrollTop=0;dialog.querySelector('.mp-panel')?.scrollIntoView({behavior:'auto',block:'start'})});dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()})})();
