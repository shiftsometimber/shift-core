(function(){'use strict';
const root=document.querySelector('[data-options-root]');
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
async function request(path,options={}){const response=await fetch(path,{credentials:'include',cache:'no-store',...options});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.message||'Treatment information could not load.');return body}
function start(){return request('/v1/treatment/pathway/start',{method:'POST'})}
function journey(){return`<nav class="tp-journey" aria-label="Treatment journey"><a href="/find-my-treatment-route">1 · YOUR DETAILS</a><b aria-current="step">2 · COMPARE</b><span>3 · ROUTE INFO</span></nav>`}
function routes(data){const all=(data.families||[]).flatMap(family=>(family.formulations||[]).map(formulation=>({family,formulation})));return all.map(({family,formulation})=>`<article class="tp-card"><span class="tp-badge is-blocked">INFORMATION ONLY</span><h2>${esc(family.name)} · ${esc(formulation.label)}</h2><p><strong>${esc(formulation.guidance?.routineLabel||`${formulation.routine} routine`)}</strong><br>${esc(formulation.guidance?.routineSummary||'The routine and exact instructions require clinical review.')}</p>${formulation.key==='daily_tablet'?'<p class="tp-route-callout"><strong>Daily tablet:</strong> still subject to suitability, strength, switching and missed-dose clinical decisions.</p>':''}<p class="tp-launch"><strong>Treatment access is closed.</strong><br><span>No price, stock or partner is being presented.</span></p><footer><a class="tp-link" href="/treatment-options/${encodeURIComponent(family.key)}/${encodeURIComponent(formulation.key)}?from=compare">Understand this route <span>→</span></a></footer></article>`).join('')}
function render(data){root.setAttribute('aria-busy','false');root.innerHTML=`${journey()}<p class="tp-kicker">ALL GOVERNED ROUTES</p><h1 class="tp-title">Compare routines clearly.</h1><p class="tp-lead">Compare catalogue-backed route information without sales pressure. This does not assess eligibility, recommend a medicine or let anyone purchase treatment.</p><div class="tp-trust"><span class="tp-badge">No account needed</span><span class="tp-badge">Clinical review required</span><span class="tp-badge is-blocked">Treatment access closed</span></div><section class="tp-grid">${routes(data)||'<article class="tp-card"><h2>No governed routes are available.</h2><p>The public journey remains safely closed.</p></article>'}</section><section class="tp-next"><div><p class="tp-kicker">YOUR NEXT USEFUL STEP</p><h2>Want a shorter list?</h2><p>Use six quick answers to match these routes to the routine that fits your life. It remains informational—not a clinical recommendation.</p></div><a class="tp-primary" href="/find-my-treatment-route">Find my route</a></section><a class="tp-back" href="/">← Back to homepage</a>`}
start().then(render).catch(error=>{root.setAttribute('aria-busy','false');root.innerHTML=`<section class="tp-error"><h1>Options did not open.</h1><p>${esc(error.message)}</p><a class="tp-primary" href="/find-my-treatment-route">Try the route finder</a></section>`})
})();
