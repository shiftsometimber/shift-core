(function(){'use strict';
const root=document.querySelector('[data-options-root]'),money=pence=>`£${(Number(pence)/100).toFixed(0)}`,esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
async function request(path,options={}){const response=await fetch(path,{credentials:'include',cache:'no-store',...options});const body=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(body.message||'Treatment information could not load.'),{status:response.status});return body}
async function start(){try{return await request('/v1/treatment/catalogue')}catch(error){if(error.status!==403||new URLSearchParams(location.search).get('entry')!=='cta')throw error;await request('/v1/treatment/pathway/start',{method:'POST'});return request('/v1/treatment/catalogue')}}
function lowest(formulation){return Math.min(...formulation.strengths.map(item=>Number(item.proposedPricePence)))}
function routes(data){const all=data.families.flatMap(family=>family.formulations.map(formulation=>({family,formulation})));return all.map(({family,formulation})=>`<article class="tp-card"><span class="tp-badge is-blocked">INFORMATION ONLY</span><h2>${esc(family.name)} · ${esc(formulation.label)}</h2><p>${esc(formulation.routine==='daily'?'A daily routine to compare after entering the treatment pathway.':'A weekly routine to compare after entering the treatment pathway.')}</p><p><strong>Proposed from ${money(lowest(formulation))}</strong><br><span class="tp-note">Prices are provisional and may rise by strength.</span></p><footer><a class="tp-link" href="/treatment-options/${encodeURIComponent(family.key)}/${encodeURIComponent(formulation.key)}">See the full proposed ladder <span>→</span></a></footer></article>`).join('')}
function render(data){root.setAttribute('aria-busy','false');root.innerHTML=`<p class="tp-kicker">GOVERNED TREATMENT PATHWAY</p><h1 class="tp-title">Options and proposed prices—before an account.</h1><p class="tp-lead">These records come from the Shift HQ catalogue. They are shown for factual comparison only. Nothing can be purchased while supplier, cost, stock, partner, clinical and claims approvals remain incomplete.</p><div class="tp-trust"><span class="tp-badge">Prices: proposed</span><span class="tp-badge">Stock: TBC</span><span class="tp-badge">Clinical assessment required</span><span class="tp-badge is-blocked">Purchase disabled</span></div><section class="tp-grid">${routes(data)}</section><section class="tp-panel"><h3>What Shift intends to include</h3><p>Clinical review, dispensing, delivery and My Timber support will only be described as included once the relevant regulated partners, service terms and costs are formally approved.</p></section><section class="tp-panel"><h3>Before any future payment</h3><p>You will see the selected-strength price, whether later strengths cost more, stock position, who provides clinical and dispensing services, what happens if a clinician does not prescribe, the approved refund process, any retained fee, incomplete-assessment handling and cancellation terms.</p></section><a class="tp-back" href="/find-my-treatment-route">← Use the route finder</a>`}
start().then(render).catch(error=>{root.setAttribute('aria-busy','false');root.innerHTML=`<section class="tp-error"><h1>Options did not open.</h1><p>${esc(error.message)}</p><a href="/find-my-treatment-route">Start with the treatment route</a></section>`})
})();
