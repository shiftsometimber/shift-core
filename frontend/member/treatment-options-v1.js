(function(){'use strict';
const root=document.querySelector('[data-options-root]');
const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
async function request(path,options={}){const response=await fetch(path,{credentials:'include',cache:'no-store',...options});const body=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(body.message||'Treatment information could not load.'),{status:response.status});return body}
async function start(){try{return await request('/v1/treatment/catalogue')}catch(error){if(error.status!==403||new URLSearchParams(location.search).get('entry')!=='cta')throw error;return request('/v1/treatment/pathway/start',{method:'POST'})}}
function routes(data){const all=data.families.flatMap(family=>family.formulations.map(formulation=>({family,formulation})));return all.map(({family,formulation})=>`<article class="tp-card"><span class="tp-badge is-blocked">INFORMATION ONLY</span><h2>${esc(family.name)} · ${esc(formulation.label)}</h2><p>${esc(formulation.routine==='daily'?'A daily routine with several strengths in the governed catalogue.':'A weekly routine with several strengths in the governed catalogue.')}</p><p class="tp-launch"><strong>Access is not open yet.</strong><br><span>Final price, stock and partner details will appear before launch.</span></p><footer><a class="tp-link" href="/treatment-options/${encodeURIComponent(family.key)}/${encodeURIComponent(formulation.key)}">Understand this route <span>→</span></a></footer></article>`).join('')}
function render(data){root.setAttribute('aria-busy','false');root.innerHTML=`<p class="tp-kicker">TREATMENT ROUTES</p><h1 class="tp-title">Compare the routine—not a sales pitch.</h1><p class="tp-lead">See the formulation routes currently held in Shift’s governed catalogue. This is factual information only: it does not decide eligibility and nothing can be purchased yet.</p><div class="tp-trust"><span class="tp-badge">No account needed</span><span class="tp-badge">Clinical assessment required</span><span class="tp-badge is-blocked">Treatment access closed</span></div><section class="tp-grid">${routes(data)}</section><section class="tp-next"><div><p class="tp-kicker">WHAT HAPPENS AT LAUNCH</p><h2>Everything important appears before payment.</h2><p>The selected-strength price, stock position, regulated clinical and dispensing partners, refund process and full terms will be visible before anyone is asked to create an order.</p></div><a class="tp-primary" href="/member/dashboard#today">See My Timber support</a></section><a class="tp-back" href="/find-my-treatment-route">← Use the route finder</a>`}
start().then(render).catch(error=>{root.setAttribute('aria-busy','false');root.innerHTML=`<section class="tp-error"><h1>Options did not open.</h1><p>${esc(error.message)}</p><a href="/find-my-treatment-route">Start with the treatment route</a></section>`})
})();
