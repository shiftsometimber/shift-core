(function(){'use strict';
const root=document.querySelector('[data-product-root]'),parts=location.pathname.split('/').filter(Boolean),familyKey=parts[1]||'',formulationKey=parts[2]||'',esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));let selected=null;
async function load(){const response=await fetch('/v1/treatment/catalogue',{credentials:'include',cache:'no-store'}),body=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(body.message||'Treatment information could not load.'),{status:response.status});return body}
function selectedBar(item){return`<section class="tp-selection" aria-live="polite"><div><p class="tp-kicker">SELECTED STRENGTH</p><h3>${esc(item.label)}</h3><p>Final price, stock and supplier details are not published until the launch gates pass.</p></div><a class="tp-primary" href="/find-my-treatment-route">Check another route</a></section>`}
function render(data){const family=data.families.find(item=>item.key===familyKey),formulation=family?.formulations.find(item=>item.key===formulationKey);if(!family||!formulation)throw new Error('That governed catalogue route does not exist.');selected=formulation.strengths[0];root.setAttribute('aria-busy','false');root.innerHTML=`<a class="tp-back" href="/treatment-options">← All treatment routes</a><p class="tp-kicker">TREATMENT ROUTE · INFORMATION ONLY</p><h1 class="tp-title">${esc(family.name)}<br>${esc(formulation.label)}</h1><p class="tp-lead">${formulation.routine==='daily'?'A daily formulation route.':'A weekly formulation route.'} No route is universally better, and clinical suitability has not been assessed.</p><div class="tp-trust"><span class="tp-badge">${esc(formulation.routine)} routine</span><span class="tp-badge">Clinical review required</span><span class="tp-badge is-blocked">Treatment access closed</span></div><section class="tp-family"><div class="tp-family-head"><div><p class="tp-kicker">CATALOGUE STRENGTHS</p><h2>Explore the available strengths.</h2></div><p class="tp-note">Selecting a strength changes the information panel. It does not start an order.</p></div><div class="tp-ladder">${formulation.strengths.map((item,index)=>`<article class="tp-dose"><button type="button" data-strength="${index}" aria-pressed="${index===0}"><strong>${esc(item.label)}</strong><span>View strength</span></button></article>`).join('')}</div></section><section class="tp-split"><article class="tp-panel"><h3>How the routine differs</h3><p>${formulation.routine==='daily'?'This route is taken daily, so remembering a regular daily dose matters.':'This route is taken weekly, so choosing and remembering a consistent weekly day matters.'}</p></article><article class="tp-panel"><h3>What a clinician reviews</h3><p>Your health history, current medicines, previous treatment, measurements and supporting evidence—not just the preference selected in the route finder.</p></article></section><details class="tp-disclosure"><summary>What would happen before treatment could be supplied?</summary><ol><li>Create a My Shift account only when treatment access opens.</li><li>Read and accept the approved sales, clinical and privacy terms.</li><li>Complete the full clinical assessment and required evidence.</li><li>An independent clinician reviews everything.</li><li>If prescribed, a regulated dispensing partner fulfils the order. If not, the published refund process applies.</li></ol></details><section data-selected>${selectedBar(selected)}</section>`;root.addEventListener('click',event=>{const button=event.target.closest('[data-strength]');if(!button)return;const item=formulation.strengths[Number(button.dataset.strength)];if(!item)return;selected=item;root.querySelectorAll('[data-strength]').forEach(node=>node.setAttribute('aria-pressed',String(node===button)));root.querySelector('[data-selected]').innerHTML=selectedBar(item)})}
load().then(render).catch(error=>{root.setAttribute('aria-busy','false');root.innerHTML=`<section class="tp-error"><h1>Start with the governed route.</h1><p>${esc(error.message)}</p><a href="/find-my-treatment-route">Find my treatment route</a></section>`})
})();
