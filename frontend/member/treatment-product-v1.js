(function(){'use strict';
const root=document.querySelector('[data-product-root]'),parts=location.pathname.split('/').filter(Boolean),familyKey=parts[1]||'',formulationKey=parts[2]||'',money=pence=>`£${(Number(pence)/100).toFixed(0)}`,esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));let selected=null;
async function load(){const response=await fetch('/v1/treatment/catalogue',{credentials:'include',cache:'no-store'}),body=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(body.message||'Treatment information could not load.'),{status:response.status});return body}
function selectedBar(item){return`<section class="tp-selection" aria-live="polite"><h3>${esc(item.label)} · proposed ${money(item.proposedPricePence)}</h3><p>Stock TBC · Supplier TBC · Clinical/claims approval TBC</p><button type="button" class="tp-primary" disabled>Purchase unavailable</button></section>`}
function render(data){const family=data.families.find(item=>item.key===familyKey),formulation=family?.formulations.find(item=>item.key===formulationKey);if(!family||!formulation)throw new Error('That governed catalogue route does not exist.');selected=formulation.strengths[0];root.setAttribute('aria-busy','false');root.innerHTML=`<a class="tp-back" href="/treatment-options">← All treatment options</a><p class="tp-kicker">FACTUAL PRODUCT INFORMATION · POST-CTA</p><h1 class="tp-title">${esc(family.name)}<br>${esc(formulation.label)}</h1><p class="tp-lead">${formulation.routine==='daily'?'A daily formulation route.':'A weekly formulation route.'} Clinical suitability has not been assessed and no formulation is universally better.</p><div class="tp-trust"><span class="tp-badge">${esc(formulation.routine)} routine</span><span class="tp-badge">Prices: proposed</span><span class="tp-badge">Stock: TBC</span><span class="tp-badge is-blocked">Not available to buy</span></div><section class="tp-family"><div class="tp-family-head"><div><p class="tp-kicker">PROPOSED STRENGTH LADDER</p><h2>See every price upfront.</h2></div><p class="tp-note">Changing strength updates the information immediately.</p></div><div class="tp-ladder">${formulation.strengths.map((item,index)=>`<article class="tp-dose"><button type="button" data-strength="${index}" aria-pressed="${index===0}"><strong>${esc(item.label)}</strong><span class="tp-price">${money(item.proposedPricePence)}</span><span>proposed</span></button></article>`).join('')}</div></section><section class="tp-split"><article class="tp-panel"><h3>Why men may consider it</h3><p>No programme claim is displayed because the supporting content and approvals are not yet present in the claims library.</p></article><article class="tp-panel"><h3>Things to think about</h3><p>Routine, evidence, limitations, side effects, storage, switching and supply information must be clinically reviewed before it is published here.</p></article></section><details class="tp-disclosure"><summary>What happens before treatment could be supplied?</summary><ol><li>Create a My Shift account.</li><li>Accept approved sales, clinical and privacy terms.</li><li>Complete the full clinical assessment and evidence upload.</li><li>An independent clinician reviews everything.</li><li>If prescribed, the regulated dispensing partner fulfils the order; if not, the approved refund process applies.</li></ol></details><section data-selected>${selectedBar(selected)}</section>`;root.addEventListener('click',event=>{const button=event.target.closest('[data-strength]');if(!button)return;const item=formulation.strengths[Number(button.dataset.strength)];if(!item)return;selected=item;root.querySelectorAll('[data-strength]').forEach(node=>node.setAttribute('aria-pressed',String(node===button)));root.querySelector('[data-selected]').innerHTML=selectedBar(item)})}
load().then(render).catch(error=>{root.setAttribute('aria-busy','false');root.innerHTML=`<section class="tp-error"><h1>Start with the governed route.</h1><p>${esc(error.message)}</p><a href="/find-my-treatment-route">Find my treatment route</a></section>`})
})();
