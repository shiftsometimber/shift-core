{
  "name": "shift-core-worker",
  "version": "3.2.0-beta.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
