# G1-012 unattended Dave production release gate — PASS

Authoritative production run: `31747143306` / job `94604287540` on main commit `ffab97e3aaae47025a9c2bcabe521541a6272542`.

The production commissioning job acquired fresh short-lived GitHub OIDC credentials before both the Fit-duration and Dave long-tail gates, preserving the existing restricted repository/actor/audience/workflow/email commissioning identity controls.

The fresh unattended Dave gate completed `PASS` and executed every currently automatable release lane in one production job:

- public discovery/trust/anonymous boundaries — PASS;
- authenticated isolation, onboarding, Today, Progress and leave/return retention — PASS;
- Grub/Fit learning and leave/return — PASS;
- Hydration, My Plans, Today and Progress safe-state — PASS;
- Progress Picture, Shift AI provenance/return behaviour and clinical boundary — PASS.

The same run also re-proved the 10/15/20/30/45/60-minute Fit duration matrix immediately before Dave.

The gate explicitly excludes the genuine real-inbox registration/verification/account-recovery clicks and external partner treatment-support journey. Those separate boundaries remain human/external and are not promoted by this PASS.

Conclusion: G1-012 is PASS. This is evidence for the synthetic unattended release-gate original audit row only; it does not by itself close B08/G5-013 or the real-inbox/external requirements.
