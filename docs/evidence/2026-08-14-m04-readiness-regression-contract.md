# M04 readiness source contract

The focused PR workflow and master `npm run check` path execute `m04-route-readiness-gate.mjs`. It pins the bounded production endpoint-readiness behavior so the proof cannot regress to a blind fixed sleep.
