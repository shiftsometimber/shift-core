// G2-012 is deliberately commissioned in its own independent browser job.
// Keeping this module inert prevents a transient G1-008 rerun from serialising
// or masking the separate Progress unit acceptance result.
